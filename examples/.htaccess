# FallbackResource requires Apache >= 2.2.12
# If you plan on caching subdirectories, you'll need to use a site-root path
FallbackResource test.php

# When files are created without extensions, use this
DefaultType "text/html; charset=utf-8"

# Use index.html as the directory index
DirectoryIndex index.html